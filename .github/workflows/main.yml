name: Service Container Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      myservice:
        image: alpine:latest
        options: --entrypoint sh
    steps:
      - name: Test Service Container
        run: |
          echo "Main job running"
          echo "Service: ${{ job.services.myservice.id }}"
          env
